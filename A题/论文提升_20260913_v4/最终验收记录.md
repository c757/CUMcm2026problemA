# 中文编辑合并稿：重新构建与检查记录

当前状态：已完成本轮中文替换、重新构建和支撑包同步；源码、两份PDF、包内重建及独立终检全部通过。本文档只记录实际完成的项目，不表示参赛队人工审阅或竞赛提交已完成。

## 文件与范围

| 文件 | 字节数 | 内容 |
|---|---:|---|
| 完整论文.pdf | 1,680,189 | 73页；摘要第1页，正文与参考文献第2—25页，共24页，完整代码附录第26—73页 |
| AI工具使用详情.pdf | 172,376 | 3页，保留真实披露和本次中文编辑记录 |
| 支撑材料_论文提升_v4.zip | 17,794,795 | 138项；137个实际文件及公开文件清单 |

24个编号公式、12幅正式图、16张表数据、5篇引用与受保护输入保持原内容；全部四问的图文覆盖通过。138项输入、数值、代码、题表、图形及代码附录与替换前逐项一致。本轮没有重算模型、重新绘图、提交Git或推送GitHub。

## 本轮实际执行

- `check_editorial.py`：退出0；与Git基线逐项核对编号公式、图号、表格数据、文献及138项受保护文件。
- `latex_paper.py doctor --engine xelatex --bibliography-backend none`：恢复依赖后退出0。
- `latex_paper.py build`：主论文及最终AI说明均退出0；最终日志无警告，源码、资源与生成PDF绑定通过。
- `editorial_validate.py`：退出0；调用技能提供的两份PDF验证命令，检查页数、公式图表、四问覆盖、引用、资源绑定、字体及页面。全部字体嵌入，无空白页，论文图为矢量PDF。
- `render_paper_qa.py`：主论文及最终AI说明退出0；查看摘要、正文、公式图表、文献及附录边界，另用`pdftoppm`渲染摘要页，退出0。
- `package_delivery.py`：退出0；138项压缩包条目和文件内容逐项核对，包内包含当前AI说明及LaTeX源文件。
- `audit_public_package.py --documents-only`：退出0；从最终支撑包解压后，137个文件与清单逐项一致。两份文档分别执行两遍XeLaTeX，4次编译均退出0。主论文73页和AI说明3页的逐页文本与公开PDF一致；主论文第1、3、7、17、20、24、29、30、73页及AI说明全部3页的渲染像素一致，最终日志无警告。本命令仅检查支撑包与文档重建，未运行数值求解。
- `git diff --check`：退出0。

检验程序统计13789字词单位。用户已批准精简重复文字，因此取消技能默认的15000字词规划量，不再为该非官方字数目标扩写；官方正文30页上限、至少20页的原质量规划，以及公式、图表、引用和PDF检查继续保留。初次验证因未显式提供附录页码和默认字数目标退出1，确认第26页为附录起始页并记录上述例外后，正式验证退出0。检查器缺少字体编码依赖的提示已通过项目内补装fontTools解决。

## 项目级依赖

本次按用户批准将25个校验过的Ubuntu软件包解包到`.work/tex_runtime/`，约492 MiB；下载包位于`.work/tex_downloads/`，约197 MiB。仅下载解包，没有运行系统安装或软件包维护脚本。

PDF检查依赖PyMuPDF 1.26.7、Pillow 11.3.0、fontTools 4.59.0位于`.work/deps/`，约100 MiB；沿用工作区已有pypdf 6.18.0。首次PyPI下载速度较慢而中止，随后使用镜像完成本地安装。所需Python模块均已确认从项目路径加载，命令行辅助脚本目录的重复提示不影响这些模块。

TeX工具、字体缓存、Python临时文件和编译临时文件均位于项目中。没有改动系统或用户目录的Python、Node、TeX配置。重建前PDF、构建清单及支撑包的恢复快照保存在`.work/pre_editorial_build/`；公开目录仅保留当前一套文件。

## 独立检查

W1与替换后的源稿语义复核：PASS。AI披露补充检查：PASS。最终W2：PASS，回执保存在`.work/editorial_W2_review.md`。独立检查者实际查看主论文第1—28页的最终渲染、最终AI说明全部3页，并核对完整PDF页数、末页文本、当前文件哈希、包内137个实际文件及重建证据。未发现P0/P1问题、必须继续修改的P2事项或新增逻辑问题。

独立检查者未参与正文编辑；此前三章的后续变化仅为多余空行归整，AI说明另将“文件上传”限定为“竞赛平台文件上传”，并以11pt排版消除仅余两行的末页，披露内容保留。终检后的公开论文、源码及支撑包不再改动。最终快照检查退出0：7份正文源稿、138项受保护文件、2份PDF与检查快照一致，支撑包内137个实际文件与项目文件逐字节一致。

## 当前文件SHA-256

```text
完整论文.pdf
3cd74be5051ecb755c11dc6ea8ff824fcbd938a1381b5c6f7980b787a8241890

AI工具使用详情.pdf
9cf73df0f0d7c89e2e4c641bb9d97683acd8ce31d93473f359623394bc8fd837

支撑材料_论文提升_v4.zip
169621a4d16e4449e2b34c4b2e63a979dc6f899ec5fc45dae7c7ea8b5803cea4
```

机器检查结果位于`.work/editorial_source_audit.json`、`.work/editorial_document_audit.json`、`.work/editorial_archive_audit.json`和`.work/editorial_public_package_audit.json`，构建命令与哈希记录位于两份PDF旁的`.build.json`。这些文件分别记录源码、文档、打包及包内重建阶段的实际状态；早期记录中的后续步骤待办仅指记录生成时点。历史数值检查保留在原记录中，本轮文档重建不冒称新增全量计算。

## 技能与使用边界

实际使用math-modeling、论文手、LaTeX工具和PDF工具，并读取项目使用指南、独立检查与截止时间协议、LaTeX格式规范和自审框架。沿用用户批准的LaTeX/PDF分支；未新建Word、调用绘图API或扩展物理机制。

格式依据[2026官方论文规范](https://www.mcm.edu.cn/html_cn/node/4cd596519c9eb9fbd866398f6df0caa3.html)，披露依据[2026官方AI规定](https://www.mcm.edu.cn/html_cn/node/fef94648f2836ab6cc81586f4c38512b.html)。已核实官方竞赛截止时间为2026年9月13日20:00（北京时间，[官方信息](https://en.mcm.edu.cn/)）；用户应自行核对所属赛区和平台的具体上传安排并确认成功。本轮不代为登录、提交或上传。

用户仍须独立理解、审阅并按真实使用情况确认最终内容。数值精度、理想模型适用范围与人工责任按论文既有说明保留。
